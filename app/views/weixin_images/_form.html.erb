<%= form_for @weixin_image, :html => {:multipart => true} do |f| %>
  <% if @weixin_image.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@weixin_image.errors.count, "error") %> prohibited this weixin_image from being saved:</h2>

      <ul>
      <% @weixin_image.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :image %><br>
    <%= f.file_field :image %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
